# Examples

Run the sample scoring workflow from the repository root:

```bash
python score_events.py sample_data/input_events.csv
```

Expected output files:

- `outputs/ranked_events.csv`
- `outputs/decision_log.csv`
- `outputs/research.db`

The sample input is synthetic and intended for portfolio review.
